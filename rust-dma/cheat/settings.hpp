#pragma once

inline bool crosshair = false;
inline ImColor crosshair_color = ImColor(1.0f, 1.0f, 1.0f, 1.0f);
inline float crosshair_size = 5.f;

inline float max_entity_distance = 300.f;

// Players
inline bool player_boxes = true;
inline bool player_names = true;
inline bool player_skeletons = true;
inline bool player_head_circle = true;
